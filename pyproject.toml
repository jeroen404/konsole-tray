[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "konsole-tray"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["PyQt6"]

[project.scripts]
konsole-tray = "konsole_tray.main:main"
